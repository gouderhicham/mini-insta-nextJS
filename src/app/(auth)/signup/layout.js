export const metadata = {
  title: "Sign Up",
  description: "Create a new Mini Insta account to start sharing posts and following your friends.",
};

export default function SignUpLayout({ children }) {
  return children;
}
