export const metadata = {
  title: "Login",
  description: "Sign in to your Mini Insta account to connect with friends and share your moments.",
};

export default function LoginLayout({ children }) {
  return children;
}
